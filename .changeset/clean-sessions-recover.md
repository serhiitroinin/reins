---
"@serhiitroinin/fold-harness": minor
---

Add versioned adapter checkpoint formats, persisted host session bindings,
runtime-owned early checkpoint writes, explicit session reset, sanitized
lifecycle diagnostics, and matching adapter conformance coverage.
