[package]
name = "reins-schema"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
publish = false
description = "Generated Rust data bindings for the Reins v1 JSON Schema"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
